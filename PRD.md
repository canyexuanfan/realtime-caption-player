# 实时字幕播放器 — 产品需求文档（PRD）

> 版本：v0.1 ｜ 日期：2026-08-27 ｜ 状态：草稿（需求评审）
> 定位一句话：**一款 Windows 优先、macOS 兼容的本地视频播放器，内置"准确 + 实时 + 中文 + 离线"字幕引擎，开箱即用，对标 PotPlayer 但补上中文实时字幕这块缺口。**

---

## 1. 背景与问题

国内主流长视频平台（爱奇艺 / B站 / 腾讯 / 优酷）和网盘官方播放器（夸克 / 百度网盘）**大多已带字幕**，但以下场景存在真实缺口：

- 本地影视文件、无字幕老片、方言内容、课程录播；
- 听障人群 / 嘈杂或深夜环境需要字幕；
- 不想把音频上传到云端（隐私）却想要准确字幕。

现状：**准确的中文 ASR 引擎（FunASR / SenseVoice）已经免费成熟**，但没有任何一款"打开任意视频 → 边播边出准确中文字幕"的零配置消费级播放器。PotPlayer 内置实时字幕用的是 Whisper（中文弱、无标点）。这条缝就是本产品的切入点。

---

## 2. 目标用户与场景

| 人群 | 场景 | 核心诉求 |
|---|---|---|
| 本地影视 / 老片爱好者 | 用挂载网盘（X/Y/Z 盘）看片 | 准确中文字幕 + 原画 + 流畅拖拽 |
| 课程 / 讲座学习者 | 看无字幕录播 | 实时字幕 + 倍速 + 可导出文稿 |
| 听障用户 | 任意视频 | 实时字幕可访问性 |
| 隐私敏感者 | 不想上传音频 | 本地离线识别 |

---

## 3. 功能需求

### 3.1 跨平台
- **Windows 优先**（主战场，作为默认交付）。
- **macOS 兼容**（次优先，架构上共用同一播放内核与字幕引擎）。
- 建议技术路线：播放内核用 **libmpv**（跨平台，Win/Mac/Linux 一致）；UI 壳用原生（Win: WinUI/Qt；Mac: SwiftUI）。

### 3.2 设为默认播放器（右键"打开方式"）
- 安装时注册常见视频扩展名（`.mp4 .mkv .avi .mov .flv .ts .m2ts .webm` 等）的文件关联。
- 支持：右键文件 → "打开方式" → 本播放器 → 勾选"始终使用此应用"。
- **Windows**：写入 `HKEY_CLASSES_ROOT` 关联 + 注册为 `Default Programs` 可选；提供"设为默认播放器"一键按钮。
- **macOS**：通过 `Info.plist` 声明 `CFBundleDocumentTypes` + 右键"始终以此方式打开"，或提供"服务(Service)"快速操作。

### 3.3 基础播放功能（一般播放器基线）
- 全格式解码（内置 FFmpeg，无需外部解码器包）。
- 播放控制：播放/暂停、进度拖拽、上一帧/下一帧、AB 循环、截图（含带字幕截图）、音轨/字幕轨切换。
- 字幕：外挂 SRT/ASS/SSA/VTT 加载、内嵌字幕选择、字号/位置/编码/延迟校准。
- 画面：亮度/对比度/饱和度、旋转、宽高比、HDR 色调映射、硬件解码（DXVA / CUDA / VideoToolbox）。
- 播放列表 / 历史记录 / 文件夹连续播放。
- 快捷键可自定义。
- 音量增益、声道切换、均衡器（可选）。

### 3.4 实时字幕（核心差异化功能）★

**需求定义**：播放任意视频时，实时将音轨识别为中文文字字幕并叠加显示，带标点、可说话人分离（可选）、可导出 SRT。

**技术方案（推荐）**：
- 引擎：**SenseVoice / Paraformer**（FunASR 生态），通过 **sherpa-onnx（Apache-2.0）** 以 ONNX 模型本地离线推理 —— 无 Python 依赖、跨平台、中文准、自带标点。
- 架构：播放器抽音轨（FFmpeg 实时 PCM）→ WebSocket/管道喂给本地 ASR 服务（sherpa-onnx / FunASR 流式）→ 末端字幕回流叠加到画面。
- 流式低延迟：VAD 分句 + 部分结果（灰字预览）+ 终稿（白字），对齐到时间戳。
- **中文优先**：默认中文模型；支持中英日韩等auto检测（SenseVoice 多语种）。
- **离线优先**：默认本地推理，不联网；可选"云端 ASR（阿里云/腾讯云/百度，有免费额度）"作为高精度模式。

**与 PotPlayer 内置方案对比**：PotPlayer 内置实时字幕基于 Whisper.cpp（中文 CER ~20%、基本无标点）；本产品用 SenseVoice（中文 CER ~2–4%、自带标点），中文准确率与可读性显著更好。

**设置项**：实时字幕开关、语言（中/英/auto）、字号、是否显示说话人、是否导出 SRT、引擎（本地/云端）。

### 3.5 倍速播放
- 范围：**0.5x – 4.0x**，步进 0.1x。
- **保音调（pitch-preserving）**：默认开启，避免加速后声音尖锐（mpv 的 `af=scaletempo` / `rubberband`）。
- 与实时字幕联动：倍速变化时字幕识别速率自适应，不影响字幕对齐。

### 3.6 网盘视频缓存策略（关键决策）★

**结论：挂载链路（rclone VFS Cache）已经具备缓存能力，播放器无需自建缓存层。**

技术依据（已核实）：rclone `mount` 提供 VFS 文件缓存（`--vfs-cache-mode off|minimal|writes|full`）。
- `off`（默认）：读直连远端、不落盘 → 视频随机拖拽会卡，**不适合看片**。
- `writes` / `full`：打开的文件先缓冲到本地缓存盘，后续读取走本地高速缓存，支持随机 seek，体验接近本地硬盘。

**推荐做法**：
1. **挂载侧**（非播放器侧）把 X/Y/Z 盘的挂载 bat 改为 `--vfs-cache-mode writes`（或 `full`），并设 `--vfs-cache-max-size`（如 50G）、`--vfs-cache-max-age`（如 1h）、`--vfs-read-ahead`（预读）。缓存目录放 SSD。
2. **播放器侧**把挂载路径当普通本地文件打开即可，**不要重复实现"预下载→播放→取消即清"**。rclone 按 LRU（最长未访问先淘汰）+ 最大体积/时长策略自动回收缓存，比"取消即清"体验更好（重看秒开）。
3. 播放器可提供一个"清理网盘缓存"按钮（调用 `rclone cache clear` 或清空缓存目录），作为可选增强，而非必要功能。

> ⚠️ 需在部署时确认当前挂载 bat 是否已设置 `vfs-cache-mode`；若仍为默认 `off`，需先改挂载配置，否则看网盘视频会卡顿。

---

## 4. 参考开源项目清单（许可证 + 可参考程度）

### 4.1 播放内核 / 播放器

| 项目 | 协议 | Stars(约) | 可参考程度 | 参考点 / 备注 |
|---|---|---|---|---|
| **mpv / libmpv** | GPLv2+（部分 LGPLv2.1、GPLv3） | 极高 | **高（内核首选）** | 播放内核 libmpv，跨平台、渲染质量顶级、脚本化。⚠️ GPL 传染，闭源产品需合规 |
| **VLC** | GPL-2.0-or-later（部分 LGPL） | 极高 | **高（功能参考）** | 全能、流媒体、无广告。中文渲染/字幕略弱于 mpv；GPL |
| **IINA** | GPLv3 | 高 | **高（macOS 参考）** | mpv 内核的 Mac 原生壳，UI 标杆 |
| **MPC-BE** | GPLv3 | 中 | 中 | Win 传统 UI 参考；偏老但轻量 | 
| **MPC-HC (clsid2 fork)** | GPLv2 | 中 | 中 | Win 原生轻量参考 |
| **SMPlayer** | GPLv2+ | 中 | 中 | mpv/mplayer 前端 UI 参考 |
| **Celluloid** | GPLv2+ | 中 | 中 | GTK 前端参考 |
| **PotPlayer** | **闭源** | — | **竞品（不引用代码）** | 功能对标；内置 Whisper 实时字幕（中文弱），本产品差异化对象 |

### 4.2 中文实时字幕 / ASR 引擎

| 项目 | 协议 | Stars(约) | 可参考程度 | 参考点 / 备注 |
|---|---|---|---|---|
| **sherpa-onnx** | Apache-2.0 | 高 | **高（离线集成首选）** | C++ 离线推理，支持 SenseVoice/Paraformer ONNX；跨平台、无 Python 依赖、无传染，桌面端最佳集成路径 |
| **FunASR** | Apache-2.0 | 36K+ | **高（ASR 引擎）** | 中文 SOTA、VAD+标点+说话人；流式/离线；Apache 无传染 |
| **SenseVoice** | Apache-2.0 | — | **高（实时字幕模型）** | FunASR 生态，中文+标点+流式，实时字幕首选模型 |
| **WhisperLiveKit** | MIT（**SimulStreaming 后端为 PolyForm Noncommercial 1.0.0**） | 10.5K | **中（架构参考）** | 实时流式转写服务架构；中文用 SenseVoice 后端；⚠️ SimulStreaming 商用需单独授权 |
| **Subtitle Edit** | MIT | 13.7K | **高（字幕 UX 参考）** | 已集成 Fun-ASR Nano / SenseVoice 本地引擎；字幕编辑/生成体验标杆，可参考其 FunASR 集成方式 |
| **pyVideoTrans** | — | 17.6K | 中 | 用 FunASR 做中文字幕+翻译 pipeline 参考 |
| **faster-whisper** | MIT | 高 | 低（外语参考） | Whisper 加速，中文弱，仅作外语补充 |
| **whisper.cpp** | MIT | 高 | 低 | PotPlayer 内置用，中文弱 |

### 4.3 基础库

| 项目 | 协议 | 可参考程度 | 备注 |
|---|---|---|---|
| **FFmpeg** | LGPL/GPL（依构建） | **高** | 解码 + 抽音轨基础；用 LGPL 构建可降传染风险 |
| **FunClip** | — | 中 | 视频切片+字幕参考 |
| **CapsWriter-Offline** | — | 中 | 离线语音输入（FunASR Paraformer）交互参考 |

---

## 5. 推荐技术架构

```
┌─────────────────────────────────────────────┐
│              原生 UI 壳 (Win/macOS)            │
│   文件关联 / 播放列表 / 设置 / 字幕叠加渲染      │
├─────────────────────────────────────────────┤
│   播放内核: libmpv (GPLv2+)                     │
│   解码 + 渲染 + 倍速(scaletempo) + 截图         │
├─────────────────────────────────────────────┤
│   音轨抽取: FFmpeg (LGPL 构建) → PCM 流         │
├─────────────────────────────────────────────┤
│   实时字幕引擎: sherpa-onnx / FunASR 流式        │
│   (SenseVoice / Paraformer ONNX, Apache-2.0)   │
│   VAD分句 → 部分结果(灰) → 终稿(白) → 时间戳     │
└─────────────────────────────────────────────┘
        ↑ 挂载网盘文件经 rclone VFS Cache 透传（无需播放器缓存）
```

**选型理由**：
- 内核 libmpv：渲染质量、跨平台、生态成熟。
- 字幕引擎 sherpa-onnx + SenseVoice：中文准、离线、Apache-2.0（无 GPL 传染），比 Whisper 中文强一个量级。
- 缓存：交给 rclone VFS Cache，播放器零额外实现。

---

## 6. 竞品对比

| 维度 | PotPlayer | VLC | 本产品 |
|---|---|---|---|
| 中文实时字幕 | Whisper（弱/无标点） | 无内置 | **SenseVoice（准/有标点）** |
| 离线隐私 | 本地 | 本地 | 本地 |
| 倍速保音调 | 有 | 有 | 有 |
| 设为默认播放器 | 有 | 有 | 有 |
| 网盘挂载文件 | 当本地文件读 | 当本地文件读 | 当本地文件读（VFS Cache） |
| 开源 | 否 | 是(GPL) | 视许可策略（见 §7） |

---

## 7. 开源协议合规要点（重要）

- **播放内核（mpv / VLC）均为 GPL**：若产品以**闭源/商业**形态分发，使用 GPL 内核需履行 GPL 义务（提供对应源码，或整体开源）。若希望闭源，需另选合规路径（开源整体 / 自研或购买商业引擎 / 用 FFmpeg 的 LGPL 构建 + 自有前端）。
- **字幕引擎（FunASR / SenseVoice / sherpa-onnx）均为 Apache-2.0**：宽松许可，可商用、可闭源集成，仅保留版权声明与 NOTICE，无传染风险。✅ 推荐。
- **WhisperLiveKit 主体 MIT，但其 SimulStreaming 后端为 PolyForm Noncommercial**：商用需单独授权；本产品用 SenseVoice 后端可避开。
- **Subtitle Edit（MIT）/ faster-whisper（MIT）/ whisper.cpp（MIT）**：宽松，可参考与集成。

> 决策建议：字幕引擎放心用 Apache/MIT 系；播放内核若走闭源路线，需提前决定 GPL 合规方案（本 PRD 默认建议**整体开源或仅做内部工具**，规避传染风险）。

---

## 8. MVP 范围

**MVP（第一版，达成用户提出的最低要求）**：
1. Windows 播放器（libmpv 内核），支持常见格式。
2. 安装即注册文件关联，可右键"打开方式 → 始终使用"设为默认。
3. 基础播放功能全集（§3.3）。
4. 倍速 0.5x–4x + 保音调。
5. 实时中文字幕：sherpa-onnx + SenseVoice 本地离线，叠加显示 + 导出 SRT。
6. 网盘文件经 rclone VFS Cache 直接播放（挂载侧配置 `writes`）。

**后续迭代**：
- macOS 版本；云端 ASR 高精度模式；说话人分离；可检索文稿/笔记导出；方言适配；"清理缓存"按钮。

---

## 9. 风险与开放问题

1. **实时字幕延迟与硬件**：本地流式 ASR 需一定算力（CPU 小模型可用，GPU 更顺）；低端机可能延迟偏高 → MVP 提供"模型大小"档位。
2. **GPL 合规**：闭源商业化前必须解决内核许可（§7）。
3. **挂载缓存配置依赖**：流畅看网盘视频依赖挂载侧 `--vfs-cache-mode writes`，需随产品提供"一键优化配置"或文档。
4. **中文极端场景**：重口音/重叠说话/强噪环境准确率低于官方，需在设置中说明预期。
5. **市场边界**：主流平台自带字幕，目标人群为本地文件/听障/学习者/隐私党，属垂直人群而非全民刚需（产品定位需精准）。

---

## 10. 附录：网盘缓存技术结论（供研发）

- rclone `mount` VFS Cache 四档：`off`（默认，不缓存，看片卡）/ `minimal` / `writes`（推荐）/ `full`。
- 推荐挂载参数示例：
  `--vfs-cache-mode writes --vfs-cache-max-size 50G --vfs-cache-max-age 1h --vfs-read-ahead 2G --cache-dir <SSD路径>`
- 播放器把挂载盘路径（如 `X:\电影\xxx.mkv`）当作本地文件打开即可；取消播放后缓存由 rclone LRU 策略回收，无需播放器干预。
- 取消即清缓存并非必需，且会降低重看体验；仅作为可选"清理缓存"按钮提供。

---
*参考项目许可证信息截至 2026-08 核实；Stars 为近似值，以各仓库实时数据为准。*
